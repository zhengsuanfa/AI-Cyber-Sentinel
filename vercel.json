{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ],
  "env": {
    "SCAN_MODE": "traditional",
    "VERCEL": "1"
  }
}

